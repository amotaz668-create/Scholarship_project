$kind: http-request
name: 'Regression: GET Universities still works'
order: 5000
method: GET
url: '{{baseUrl}}/api/universities'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function() {
        pm.response.to.have.status(200);
      });
