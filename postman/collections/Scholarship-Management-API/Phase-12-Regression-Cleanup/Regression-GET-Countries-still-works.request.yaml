$kind: http-request
name: 'Regression: GET Countries still works'
order: 4000
method: GET
url: '{{baseUrl}}/api/countries'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function() {
        pm.response.to.have.status(200);
      });
