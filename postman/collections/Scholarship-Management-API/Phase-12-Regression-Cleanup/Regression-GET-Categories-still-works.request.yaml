$kind: http-request
name: 'Regression: GET Categories still works'
order: 3000
method: GET
url: '{{baseUrl}}/api/categories'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function() {
        pm.response.to.have.status(200);
      });
