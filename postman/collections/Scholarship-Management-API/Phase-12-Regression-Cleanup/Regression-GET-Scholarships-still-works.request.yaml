$kind: http-request
name: 'Regression: GET Scholarships still works'
order: 6000
method: GET
url: '{{baseUrl}}/api/scholarships'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function() {
        pm.response.to.have.status(200);
      });
