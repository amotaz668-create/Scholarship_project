$kind: http-request
name: 'POST Create Scholarship - Student Token (403)'
order: 6000
method: POST
url: '{{baseUrl}}/api/scholarships'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{studentToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "title": "Test"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 403", function() {
        pm.response.to.have.status(403);
      });
