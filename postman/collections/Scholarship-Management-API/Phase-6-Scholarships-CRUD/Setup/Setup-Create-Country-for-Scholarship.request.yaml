$kind: http-request
name: 'Setup: Create Country for Scholarship'
order: 2000
method: POST
url: '{{baseUrl}}/api/countries'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{employeeToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "name": "United States",
      "code": "US"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 201", function() {
        pm.response.to.have.status(201);
      });
      const json = pm.response.json();
      if (json.data && json.data._id) {
        pm.environment.set("countryId", json.data._id);
      }
