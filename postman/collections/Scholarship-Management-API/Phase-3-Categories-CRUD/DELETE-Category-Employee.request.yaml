$kind: http-request
name: 'DELETE Category (Employee)'
order: 12000
method: DELETE
url: '{{baseUrl}}/api/categories/{{categoryId}}'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{employeeToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function() {
        pm.response.to.have.status(200);
      });
